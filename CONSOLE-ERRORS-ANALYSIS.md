# Анализ ошибок консоли браузера

## 🔍 Найденные проблемы

### 1. **SSR/Hydration ошибки** (КРИТИЧНО)
**Проблема**: Использование `window`, `document`, `localStorage` без проверки на SSR
**Файлы**:
- `src/app/page.tsx` (строки 28-29, 62, 65-66, 83, 98, 110-111, 125-126)
- `src/app/test/page.tsx` (строки 26, 32, 71, 78)
- `src/app/layout.tsx` (строки 72, 87)

**Ошибки**:
- `ReferenceError: window is not defined` (SSR)
- `ReferenceError: document is not defined` (SSR)
- `ReferenceError: localStorage is not defined` (SSR)

**Решение**: Добавить проверки `typeof window !== 'undefined'` перед использованием

---

### 2. **Отладочные логи в production** (ВЫСОКИЙ ПРИОРИТЕТ)
**Проблема**: Fetch-запросы к debug-серверу остались в коде
**Файлы**:
- `src/app/page.tsx` (строки 25, 33, 39, 46, 58)
- `src/app/about/page.tsx` (строки 10, 74, 86, 100)
- `src/app/api/content/homepage_services/route.ts` (строки 8, 16, 22)

**Ошибки**:
- `Failed to fetch: http://127.0.0.1:7243/...` (в production)
- Network errors в консоли

**Решение**: Удалить все `#region agent log` блоки или обернуть в `process.env.NODE_ENV === 'development'`

---

### 3. **Потенциальные null/undefined ошибки** (СРЕДНИЙ ПРИОРИТЕТ)
**Проблема**: Отсутствие проверок на существование данных перед `.map()`, `.find()`, `.filter()`
**Файлы**:
- `src/app/page.tsx` (строка 210: `services.map` - может быть undefined)
- `src/app/about/page.tsx` (строка 71: `practitioners.map` - может быть undefined)
- `src/app/contact/page.tsx` (строка 28: `contact.methods.map` - может быть undefined)
- `src/app/blog/page.tsx` (строки 66-69: `blogPosts.filter` - может быть undefined)

**Ошибки**:
- `Cannot read property 'map' of undefined`
- `Cannot read property 'find' of undefined`

**Решение**: Добавить проверки `?.map()` или `array?.length ? array.map() : []`

---

### 4. **Ошибки загрузки изображений** (СРЕДНИЙ ПРИОРИТЕТ)
**Проблема**: Изображения могут не загружаться, вызывая ошибки в консоли
**Файлы**:
- `src/app/about/page.tsx` (строки 79-103: изображения practitioners)
- `src/components/Header.tsx` (строки 30-45: логотип)

**Ошибки**:
- `Failed to load image: /images/practitioners/mike.png`
- `Failed to load image: /images/logo-resursnie-orig2.png`

**Решение**: Улучшить обработку ошибок загрузки изображений (уже есть `onError`, но можно улучшить)

---

### 5. **Console.error в Header** (НИЗКИЙ ПРИОРИТЕТ)
**Проблема**: `console.error` в production коде
**Файл**: `src/components/Header.tsx` (строка 35)

**Ошибки**: Загрязняет консоль в production

**Решение**: Удалить или обернуть в `if (process.env.NODE_ENV === 'development')`

---

### 6. **Потенциальные проблемы с типами** (НИЗКИЙ ПРИОРИТЕТ)
**Проблема**: Использование `any` типов может скрывать ошибки
**Файлы**:
- `src/app/test/page.tsx` (строки 18, 20, 22, 35, 41, 43, 56)

**Ошибки**: TypeScript не может проверить корректность типов

**Решение**: Заменить `any` на конкретные типы

---

## 📋 TODO для исправления

### Приоритет 1 (КРИТИЧНО - блокирует работу)
- [ ] Исправить SSR ошибки с `window`/`document`/`localStorage`
- [ ] Удалить отладочные логи из production кода

### Приоритет 2 (ВЫСОКИЙ - влияет на UX)
- [ ] Добавить проверки на null/undefined перед `.map()`, `.find()`, `.filter()`
- [ ] Улучшить обработку ошибок загрузки изображений

### Приоритет 3 (СРЕДНИЙ - улучшение качества)
- [ ] Удалить `console.error` из production кода
- [ ] Заменить `any` типы на конкретные типы

---

## 🎯 Рекомендации

1. **Сначала исправить SSR ошибки** - они могут полностью ломать приложение
2. **Удалить отладочные логи** - они вызывают network errors в production
3. **Добавить проверки данных** - предотвратит runtime ошибки
4. **Улучшить обработку изображений** - улучшит UX
