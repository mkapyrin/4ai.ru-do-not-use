# Настройка GitHub Pages для деплоя через pnpm

## Шаги настройки в GitHub репозитории:

### 1. Включение GitHub Pages
1. Зайдите в репозиторий на GitHub
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** выберите **GitHub Actions**

### 2. Настройка ветки по умолчанию (опционально)
- Убедитесь, что основная ветка - `main`
- Или измените workflow если используете другую ветку

### 3. Первый деплой
После настройки Pages:о
```bash
git push origin main
```

Это автоматически запустит workflow `.github/workflows/deploy.yml`

## Workflow особенности:

### ✅ **Что делает workflow:**
- Устанавливает **Node.js 18** (рекомендуемая версия)
- Использует **pnpm** для установки зависимостей
- Собирает проект командой `pnpm build`
- Деплоит содержимое папки `dist/` в GitHub Pages

### 🔧 **Преимущества pnpm для деплоя:**
- **Быстрая установка** зависимостей (экономия времени CI)
- **Эффективное использование диска** (shared dependencies)
- **Надежная блокировка версий** через `pnpm-lock.yaml`

## Проверка деплоя:

### Способы проверки:
1. **GitHub Actions tab** - мониторинг процесса сборки
2. **Pages settings** - после успешного деплоя покажет URL
3. **Actions logs** - детальная информация о сборке

### Возможные проблемы:
- Если деплой не запускается: проверьте настройки Pages в репозитории
- Ошибки сборки: проверьте логи в GitHub Actions
- Проблемы с pnpm: убедитесь что Node.js версия >= 16

## Production URL:
После успешного деплоя сайт будет доступен по адресу:
```
https://mkapyrin.github.io/4ai.ru/
```

**GitHub репозиторий:** https://github.com/mkapyrin/4ai.ru

## Локальная разработка:
```bash
# Разработка с авто-перезагрузкой
pnpm dev

# Предварительный просмотр production сборки
pnpm preview

# Сборка для продакшна
pnpm build